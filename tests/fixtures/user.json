{
  "type": "object",
  "properties": {
    "id": { "$ref": "id-schema.json", "param": "userId" },
    "email": { "$ref": "email.schema" }
  },
  "required": ["id", "email"],
  "routes": ["GET /user/:userId"]
}
